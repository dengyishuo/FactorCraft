% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_risk_adj_mom.R
\name{add_risk_adj_mom}
\alias{add_risk_adj_mom}
\title{Add Risk-Adjusted Momentum (RAM)}
\usage{
add_risk_adj_mom(
  data,
  close_col = "close",
  new_col = "ram",
  n = c(5, 10, 20),
  type = c("continuous", "discrete"),
  risk_type = c("vol", "VaR", "CVaR"),
  p = 0.95,
  na.pad = TRUE,
  append = TRUE,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{data}{Standard long data from get_data()}

\item{close_col}{Close price column}

\item{new_col}{Prefix for final RAM factor (default: "ram")}

\item{n}{Lookback periods (default: c(5,10,20))}

\item{type}{Return type: "continuous" or "discrete"}

\item{risk_type}{"vol" (std), "VaR", "CVaR"}

\item{p}{Confidence level for VaR/CVaR (default 0.95)}

\item{na.pad}{Pad leading NAs}

\item{append}{Append to data or return clean factors}

\item{output}{"tibble" or "data.frame"}
}
\value{
Tibble/data.frame with columns:
\item{\code{ret_{n}}}{: Period return (same as add_return)}
\item{\code{vol_std_{n}}}{: Rolling volatility (same as add_vol_std)}
\item{\code{ram_{n}}}{: Risk-adjusted momentum = ret / vol_std}
}
\description{
Calculate risk-adjusted momentum using return / risk.
Uses consistent naming with add_return() and add_vol_std().
}
